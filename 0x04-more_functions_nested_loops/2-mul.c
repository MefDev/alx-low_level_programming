#include "main.h"

/**
 * mul - Multiply two numbers
 * @a: The first number
 * @b: The second number
 * Return: (1) if digit 0 otherwise
 */
int mul(int a, int b)
{
	int result = 0;

	result = a * b;
	return (result);
}
